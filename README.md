# next-word-prediction-GRU
A deep learning based next-word prediction application built using TensorFlow, Keras, GRU, and Streamlit. The model analyzes text sequences and predicts the next probable word using NLP sequence modeling and recurrent neural networks.
